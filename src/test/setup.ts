/**
 * Vitest Browser Test Setup
 *
 * This file runs before all tests to set up the WASM environment.
 */

// No global setup needed for now - WASM is loaded per-test
// to ensure isolation between tests.

export {}
